import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  formContainer: {
    flex: 1,
    justifyContent: "space-between",
    gap: 12,
  },
  formFields: {
    gap: 12,
  },
});